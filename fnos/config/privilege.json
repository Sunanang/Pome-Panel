{
  "network": {
    "outbound": false,
    "listenUnix": true,
    "listenTcp": true,
    "tcpPortFixed": true,
    "note": "设备同步端口由安装向导 wizard_port 写入，启动时固定绑定；禁止在代码里写死 5001"
  },
  "filesystem": {
    "dataDir": true,
    "runtimeDir": true
  },
  "gateway": {
    "trustTrimHeaders": true,
    "stripClientTrimHeaders": true,
    "note": "仅 Unix socket / 受控反代入口可信 X-Trim-*；网关须清客户端同名头后重注"
  }
}
